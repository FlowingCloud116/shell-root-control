# Shell Root Control：使用说明

这份说明可直接交给其他 Codex/AI 对话。目标是让新的对话在不了解模块开发历史的情况下，安全使用当前设备的 Shell Root。

## 模块用途

设备安装了通用 Magisk 模块 `Shell Root Control`：

- 模块 ID：`shell_root_control`
- 控制命令：`rootctl`
- 默认状态：每次开机为 `off`
- 无线调试：每次开机自动开启，并每 5 秒检查和恢复一次
- 适配版本：Magisk 30.7，版本代码 30700
- 认证方式：无额外认证，依赖 Android 的 ADB 主机授权

不要直接要求用户反复点击 Magisk 的 Shell 授权弹窗。模块正常时，使用 `rootctl` 自助开关。

## 无线调试行为

- 模块只使用 Android 11+ 配对式无线调试，不启用传统 TCP 5555。
- 每次开机完成后会自动设置 `adb_wifi_enabled=1`。
- 用户在系统设置中手动关闭后，最多约 5 秒会被重新开启，这是所有者确认的持续强制行为。
- 模块不会修改配对密钥、USB 调试开关、开发者选项或 Wi-Fi 网络。
- 新电脑仍需由所有者在手机的无线调试页面手动配对。
- 卸载后保留无线调试当前状态；需要关闭时，等模块停止运行后再从系统设置关闭。

## 标准操作流程

### 1. 确认设备在线

```text
adb devices -l
```

必须看到目标设备状态为 `device`，不能是 `offline` 或空列表。

### 2. 读取当前状态

```text
adb shell rootctl status
```

可能返回：

- `off`：新的 Shell `su` 会被拒绝。
- `on`：新的 Shell `su` 可以取得 Root。
- `blocked`：Magisk 全局设置禁止 ADB Root，不能擅自修改全局设置。

如果命令不存在或提示控制服务未就绪，停止 Root 操作，不要连续调用 `su`。先让所有者检查 Magisk 中的模块状态。

### 3. 临时开启 Root

```text
adb shell rootctl on
```

命令通常需要约 4 秒。明确返回 `on` 后，再做一次最小验证：

```text
adb shell su -c id
```

只有看到 `uid=0(root)` 才能继续执行需要 Root 的任务。

### 4. 执行获准的 Root 操作

只执行当前任务明确授权的操作。不要因为已经取得 Root 就扩大范围，不要清除应用数据、卸载应用、删除其他 Magisk 模块或修改无关系统配置。

优先使用前台、可等待完成的 Root 命令。不要留下无法追踪的长期后台 Root 进程。

### 5. 完成后关闭 Root

先等待所有 Root 命令执行完，再运行：

```text
adb shell rootctl off
adb shell rootctl status
```

必须确认返回 `off`。如需严格验收，可以新建一次 `su` 请求并确认没有得到 `uid=0(root)`。

`rootctl off` 只阻止后续新的 `su`，不会夺走已经运行中的 Root 进程权限。因此关闭前必须先结束现有 Root 命令。

## 重启行为

即使关机前为 `on`，重新启动后也会自动恢复 `off`。重启后的正确检查顺序是：

```text
adb shell rootctl status
```

先确认返回 `off`，不要先用高频 `su` 请求探测。

## 安装包和源码

如果另一个项目需要复制模块，可使用：

```text
tools/magisk-shell-rootctl/
tools/magisk-shell-rootctl/dist/shell-root-control-v1.2.0.zip
```

目录中的 `v1.1.0` 是旧的 Root-only 回滚包；不要用它替代 `v1.2.0`，否则不会自动维护无线调试。

模块是独立 Shell/Magisk 脚本，不依赖 FlowChat、Gradle、JDK 或 Android SDK。

安装前必须确认：

- Magisk 精确为 30.7（30700）。
- Android 系统报告支持配对式无线调试。
- 系统中没有另一个模块占用 `/system/bin/rootctl`。
- 没有同时启用旧模块 ID `flowchat_shell_rootctl`。
- ZIP 的电脑端和设备端 SHA-256 一致。

不要使用 `PRAGMA table_info(policies)` 探测当前 Magisk CLI；目标设备已确认该多行查询会使 CLI 通道失败。模块使用单行 `sqlite_master` 查询进行兼容检查。

## 卸载与异常处理

正常卸载：

1. 执行 `rootctl off`。
2. 确认 `rootctl status` 返回 `off`。
3. 在 Magisk App 中卸载 `Shell Root Control`。
4. 重启设备。

卸载脚本会固定将 UID 2000 设为拒绝。

卸载不会关闭无线调试，这是所有者确认的数据保留规则。

如果 `rootctl` 不可用，不要删除整个 `/data/adb/modules`，也不要运行会移除全部模块的命令。让所有者在 Magisk App 中临时允许 Shell Root，只移除模块 ID `shell_root_control`，然后重启。

## 必须牢记的安全边界

- 任何已获 ADB 授权的主机都能执行 `rootctl on`。
- 模块会持续开启无线调试；ADB 主机授权和当前网络可达性是外部边界。
- 模块不会限制 Root 命令内容；操作范围仍由当前任务授权决定。
- 不要在报告、日志或截图中泄露设备上的密钥、聊天内容或其他私人数据。
